import { render, screen } from "@testing-library/vue";
import MainNav from "../../../src/components/MainNav.vue";
import { describe } from "vitest";

describe("MainNav", () => {
  it("displays company name", () => {
    // render(MainNav).debug;
    // screen.debug;
  });
});
